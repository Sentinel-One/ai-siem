# Direct dependencies for the Scalyr agent monitor plugins.
# Compile the hash-locked requirements.txt from this file:
#   pip-compile --generate-hashes --output-file requirements.txt requirements.in
requests==2.32.4
pandas==2.2.3
numpy==1.26.4
geoip2==4.8.1
tabulate==0.9.0
